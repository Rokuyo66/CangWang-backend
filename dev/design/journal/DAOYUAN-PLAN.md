# 道緣上線計畫：好感一定換得到東西

> 2026-09-23 · 對應前端 b61（觀堂角色卡事件泡泡）
> 核心：**鼓勵多卜卦、多聊天累積道緣。** 所以道緣每漲到一個點，都要有東西可拿；
> 絕不能出現「道緣到了，點進去是一句『尚未開放』」。

---

## 1. 現況盤點（上線前的缺口）

| 角色 | 章 | 門檻 | 內容 | published | 問題 |
|---|---|---|---|---|---|
| 大師兄 | c1 代理 | 0 | ✅ 6 幕＋3 選項＋記憶 | true | — |
| 大師兄 | c2 | 300 | ❌ scenes 空 | **true** | 目錄看得到、點了進不去 |
| 大師兄 | c3 | 500 | ❌ scenes 空 | **true** | 同上 |
| 師妹 | — | — | ❌ 無 | — | 三人裡兩人沒有任何事件 |
| 觀貓 | — | — | ❌ 無 | — | 同上 |

後端發獎只做了一件事：`event_finish` 第一次了結時寫入 `rewards.memory`（character_memories, source='event'）。
`rewards.title` 由 0038 的 character_titles 另管；**靈石、頭像、來信都還沒接。**

前端 `STORY_OPEN=false`，所以現在整條事件線是關的，泡泡也不會亮。

---

## 2. 道緣經濟：多久會碰到門檻

每日來源（每位角色分開算，簽到三人都加）：

| 來源 | 道緣 | 備註 |
|---|---|---|
| 聊天（AI 回覆） | +1／句 | 免費 8 句／日（全角色共用），觀微 12、知己 30、藏往 100 |
| 起卦（歸到該角色） | +3 | 追問 +2 |
| 簽到 | 7 日循環第 3 天 +5、第 7 天 +10 | ≈ +2.1／日，三人都加 |

推算（專注一位角色）：

| 玩家型態 | 每日約 | 到 300 | 到 500 | 到 800 |
|---|---|---|---|---|
| 輕度免費（3 句＋隔天一卦＋簽到） | ~7 | 43 天 | 72 天 | 115 天 |
| 重度免費（8 句＋1 卦 1 追問＋簽到） | ~15 | 20 天 | 34 天 | 54 天 |
| 知己（30 句＋1 卦 1 追問） | ~37 | **8 天** | 14 天 | 22 天 |

結論：
1. **300 → 500 → 800 間距太大。** 輕度玩家頭一個半月除了 c1 什麼都拿不到，恰好是流失最高的時段。要在中間補小節點（§3）。
2. **付費玩家第 8 天就到 300。** 所以開放 STORY_OPEN 那天，c2 必須已經寫好；c3 最晚 +2 週。
3. **老玩家可能已經超過門檻。** 開放前先跑：
   ```sql
   select character_id,
          count(*) filter (where favor >= 300) as ge300,
          count(*) filter (where favor >= 500) as ge500,
          count(*) filter (where favor >= 800) as ge800
     from user_character group by 1;
   ```
   有人 ≥ 某門檻、那一章卻沒內容 → 那一章不准開放，先補內容。

---

## 3. 節點設計：大節點演劇情，小節點寄一封信

把道緣切成兩種節點。小節點是純文字、成本低，負責填滿大節點之間的空檔；大節點才需要美術與劇本。

### 小節點（每 50：50／100／150／200／250／350／…）— 「他寫了一封信給你」

- 形式：角色口吻的站內信（mail kind=character），夾 **5～20 靈石**。沿用 0063 的 `mail_send`＋`mail_claim`，不必新做 UI。
- 內容：一段只有到了這個熟度他才會說的話，**同時寫入一條額外記憶**（character_memories, source='milestone'）。
  之後聊天時他會引用這段（「上次跟你說過後院那棵樹……」），讓玩家感覺到「他記得」。這就是額外記憶的用途：不是收集品，是會回到對話裡的東西。
- 觸發：後端在道緣跨過節點時寄出（見 §5），不是前端。
- 量：三人 × 每 50 一封，50 到 800 共 16 個點，扣掉 300／500／800 三個大節點 → **每人 13 封，共 39 封文字**。可分批寫：上線只需寫到 250（每人 5 封，共 15 封）。

### 大節點（300 相熟／500 相知／800 知己）— 劇情章

| 獎勵 | 300 相熟 | 500 相知 | 800 知己 |
|---|---|---|---|
| 劇情章 | 第二章 | 第三章 | 第四章 |
| 身分（title） | ✅ | ✅ | ✅ |
| 額外記憶 | 章末那一句 | 同 | 同 |
| 記憶額度 | +1 格（該角色） | +1 格 | +2 格 |
| 靈石 | 30 | 50 | 100 |
| 外觀 | — | 動態頭像框 | 專屬動態頭像＋主題色一套 |

記憶額度加格是「好感換功能」：他能記住你更多事——直接呼應「紀錄手帳本」這個核心。

### 起點（0 初識）— 第一章

三人各一章，**入場選完引路人後就能演**。現在只有大師兄有。師妹、觀貓的第一章是上線必備，不然三人只有一人有故事，跟入場「我們有角色」的承諾對不上。

---

## 4. 上線閘門（STORY_OPEN 改 true 之前全部打勾）

- [ ] 三人第一章（門檻 0）都寫好、美術到位
- [ ] 三人第二章（300）都寫好——付費玩家第 8 天就到
- [ ] 沒寫好的章 `published=false`（c2／c3 現在是 true、scenes 空，要先關）
- [ ] §2 的 SQL 跑過：有人已過門檻的章，內容都在
- [ ] 小節點信件至少寫到 250（每人 5 封）
- [ ] `event_finish` 會發靈石／記憶額度（§5）
- [ ] 前端泡泡規則不變：**只在「下一章道緣夠、而且 scene_count>0」時亮**（`storyReady()`，b61 已實作）——
      即使哪天漏關了一章，泡泡也不會把人帶去空章前面

之後每一章的閘門：寫好 → published=true。前端不必重出 APK。

---

## 5. 要做的後端工程

| # | 項目 | 說明 |
|---|---|---|
| B1 | 0065：關掉空章 | `update character_events set published=false where scenes = '[]'::jsonb` |
| B2 | 0066：`favor_milestones` 表 | (character_id, at, subject, body, lingshi, memory)。一列一封信 |
| B3 | 跨節點寄信 | 在所有加道緣的地方（chat.ts、pipeline.ts addCultivation、簽到）統一走一支 `add_favor(user, cid, n)`：更新後比對舊值與新值之間跨過的 `favor_milestones`，逐封 `mail_send`＋寫 memory。以 (user, cid, at) 做唯一鍵防重寄 |
| B4 | `event_finish` 發全套 | rewards 增加 `lingshi`、`memory_slots`、`avatar`；第一次了結才發（沿用 firstTime） |
| B5 | 記憶額度讀加格 | 方案額度＋已了結章的 memory_slots 總和 |

B3 是關鍵：現在加道緣散在三處，各自 `update ... favor + n`。收成一支之後，節點判斷、封頂、寄信都只寫一次。

---

## 6. 內容與美術產量

| 類型 | 上線必備 | 之後 |
|---|---|---|
| 劇情章（6～10 幕＋3 選項） | 師妹 c1、觀貓 c1、三人 c2 → **5 章** | 三人 c3、c4 → 6 章 |
| 小節點信 | 三人 × 50～250 → **15 封** | 24 封 |
| 場景圖（1280×1920） | 劇情用到的：前廳、香案、後院、藏書閣 | 夜景版 |
| 立繪（900×1200 半身） | 三人各一張（事件、入場、談心都吃這張） | 表情差分 |
| 頭像框／專屬動態頭像 | — | 500、800 節點用 |

美術規格同 `cangwang-web/assets/art/README.md`。

---

## 7. 建議順序

1. B1（今天就能跑，把空章藏起來）
2. 寫師妹、觀貓第一章＋三人第二章
3. B2＋B3＋小節點信（這一步就算劇情還沒齊也能先上：玩家馬上感覺到「好感有回應」）
4. B4＋B5
5. 閘門全勾 → `STORY_OPEN=true` 出 APK
